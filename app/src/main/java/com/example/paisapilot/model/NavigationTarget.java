package com.example.paisapilot.model;

/**
 * NavigationTarget
 *
 * Enum representing where the splash screen should navigate next.
 */
public enum NavigationTarget {
    LOGIN,
    PROFILE_SETUP,
    MAIN
}
