<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PaisaPilot" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main colors for Dark mode -->
        <item name="colorPrimary">@color/primary_purple</item>
        <item name="colorSecondary">@color/accent_green</item>
        <!-- The user requested light/white splash specifically -->
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:statusBarColor">@color/splash_bg</item>
    </style>

    <!-- Ensure the Splash theme is light even in dark mode per user request -->
    <style name="Theme.PaisaPilot.Splash" parent="Theme.PaisaPilot">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Android 12+ System Splash Screen -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/ic_paisapilot_logo</item>
    </style>
</resources>
